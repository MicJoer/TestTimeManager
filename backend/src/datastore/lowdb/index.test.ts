import { LowDb } from './index'
(() => {
    const db = new LowDb()
    // db.addHourEntry( 'testuser', new Date(),[
    //     { unixStartTime: 123, unixEndTime: 124 }
    // ]);
    // const data = db.getHoursByDate('testuser', new Date())
    // console.log({data})

})();